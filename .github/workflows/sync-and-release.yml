# ============================================================
# OpenClaw 汉化发行版 - 自动同步与发布工作流
# 武汉晴辰天下网络科技有限公司 | https://qingchencloud.com/
# ============================================================

name: 同步上游 & 自动发布

on:
  schedule:
    # 每小时检查一次上游更新
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      force_release:
        description: '强制发布（即使无更新）'
        required: false
        default: 'false'
        type: boolean

env:
  UPSTREAM_REPO: openclaw/openclaw
  NODE_VERSION: '22'

jobs:
  # ==================== 检测上游更新 ====================
  check-upstream:
    runs-on: ubuntu-latest
    outputs:
      has_updates: ${{ steps.check.outputs.has_updates }}
      upstream_version: ${{ steps.check.outputs.version }}
      commit_count: ${{ steps.check.outputs.commit_count }}
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 检测上游更新
        id: check
        run: |
          # 获取上游最新版本
          UPSTREAM_VERSION=$(curl -s https://registry.npmjs.org/openclaw/latest | jq -r '.version')
          echo "上游版本: $UPSTREAM_VERSION"
          echo "version=$UPSTREAM_VERSION" >> $GITHUB_OUTPUT
          
          # 检查是否需要更新
          LAST_SYNCED=$(cat .last-synced-version 2>/dev/null || echo "0.0.0")
          echo "上次同步版本: $LAST_SYNCED"
          
          if [ "$UPSTREAM_VERSION" != "$LAST_SYNCED" ] || [ "${{ inputs.force_release }}" == "true" ]; then
            echo "has_updates=true" >> $GITHUB_OUTPUT
            echo "✅ 检测到新版本，准备同步"
          else
            echo "has_updates=false" >> $GITHUB_OUTPUT
            echo "⏸️ 无新版本"
          fi

  # ==================== 构建与发布 ====================
  build-and-release:
    needs: check-upstream
    if: needs.check-upstream.outputs.has_updates == 'true'
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          registry-url: 'https://registry.npmjs.org'

      - name: 安装 pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: 克隆上游代码
        run: |
          git clone --depth 1 https://github.com/${{ env.UPSTREAM_REPO }}.git openclaw
          echo "✅ 上游代码已克隆"

      - name: 应用汉化补丁
        run: |
          node cli/index.mjs apply --verbose
          echo "✅ 汉化补丁已应用"

      - name: 验证汉化结果
        run: |
          node cli/index.mjs verify
          echo "✅ 汉化验证通过"

      - name: 构建 OpenClaw
        working-directory: openclaw
        run: |
          pnpm install
          pnpm run build
          echo "✅ 构建完成"

      - name: 准备发布版本
        id: prepare
        run: |
          UPSTREAM_VERSION="${{ needs.check-upstream.outputs.upstream_version }}"
          DATE=$(date +%Y%m%d)
          RELEASE_VERSION="${UPSTREAM_VERSION}-zh.${DATE}"
          
          echo "release_version=$RELEASE_VERSION" >> $GITHUB_OUTPUT
          echo "📦 发布版本: $RELEASE_VERSION"
          
          # 更新 package.json 版本
          cd openclaw
          npm version $RELEASE_VERSION --no-git-tag-version --allow-same-version || true
          
          # 修改包名为汉化版
          jq '.name = "@qingchencloud/openclaw-zh"' package.json > tmp.json && mv tmp.json package.json
          jq '.description = "OpenClaw 汉化发行版 - 武汉晴辰天下网络科技有限公司"' package.json > tmp.json && mv tmp.json package.json

      - name: 发布到 npm
        working-directory: openclaw
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: 创建 GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.prepare.outputs.release_version }}
          name: OpenClaw 汉化版 v${{ steps.prepare.outputs.release_version }}
          body: |
            ## 🦞 OpenClaw 汉化发行版
            
            **版本**: v${{ steps.prepare.outputs.release_version }}
            **上游版本**: ${{ needs.check-upstream.outputs.upstream_version }}
            **发布时间**: ${{ github.event.repository.updated_at }}
            
            ### 📦 安装方式
            
            ```bash
            npm install -g @qingchencloud/openclaw-zh@latest
            ```
            
            或使用一键安装脚本：
            
            ```bash
            # Linux/macOS
            curl -fsSL https://cdn.jsdelivr.net/gh/1186258278/OpenClawChineseTranslation@main/install.sh | bash
            
            # Windows
            powershell -c "irm https://cdn.jsdelivr.net/gh/1186258278/OpenClawChineseTranslation@main/install.ps1 | iex"
            ```
            
            ### 🔗 相关链接
            
            - 官网: https://openclaw.qt.cool/
            - 文档: https://github.com/1186258278/OpenClawChineseTranslation
            
            ---
            
            **武汉晴辰天下网络科技有限公司** | https://qingchencloud.com/
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 更新同步版本记录
        run: |
          echo "${{ needs.check-upstream.outputs.upstream_version }}" > .last-synced-version
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .last-synced-version
          git commit -m "chore: 更新同步版本至 ${{ needs.check-upstream.outputs.upstream_version }}" || true
          git push || true
